{
  "perr + perror": {
    "prefix": [
      "perr",
      "perror"
    ],
    "body": [
      "if err != nil {",
      "\tctx.Log.Error(fmt.Sprintf(\"${1:}: %v\", err), nil)",
      "\treturn ${2:nil, err}",
      "}"
    ]
  },
  "err": {
    "prefix": "err",
    "body": [
      "if err != nil {",
      "\treturn err",
      "}"
    ]
  }
}
